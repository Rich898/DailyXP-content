# PRIVATE — performance data, names, state, secrets. NEVER in the public repo.
# Destination is the private repo (stood up this weekend, alongside the ledgers).
# NOTE: git does not support trailing comments on pattern lines — keep patterns bare.

work/
history/
targets/
schedule.json
SCHEDULE.md
tools/schedule_build.py
publish_log.jsonl
*_mastery_ledger.md
.ghtoken
.token
tools/__pycache__/
*.pyc

# shell build artifacts — NEVER commit (stamped pages carry real names; node_modules is junk).
# shell/*/ ignores ALL subdirectories (node_modules, any stamped per-student build, test build);
# only the template + test_*.js at shell/ root are tracked.
shell/*/
shell/package.json
shell/package-lock.json
shell/y9.json
